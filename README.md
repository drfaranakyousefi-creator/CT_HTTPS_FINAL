# final project 
implemeted the transformer architecture instead of Dense and sparse networks 
implemented the CapsNet instead of simple MLP for the server network 
implemented the knoledge transfer mechanisam 
